# Learnings — <domain name>

**Open when touching:** <the files, subsystems, or task shapes that should trigger reading this index>.
One line per learning — *trigger → consequence*; the leaf file holds the full story. No findings, no numbers, no status. Longer pre-split summaries: `[[archive/archive_learnings_index_longform_<date>|archive longform]]`.

- `[[<learning-name>]]` — <date>: <trigger — the situation that makes this relevant> → <consequence — what happens if ignored, in one clause>. <Optional: the one-line rule.>
